This program, written in MIPS assembly, represents several simple binary search tree operations,
including node creation, node insertion, and traversals.
This program was run through the MARS MIPS assembler.
